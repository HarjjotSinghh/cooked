# Goose

## Install

```bash
npx skills add HarjjotSinghh/cooked --skill cooked -a goose -g -y
```

| Scope | Path |
|---|---|
| Global | `~/.config/goose/skills/cooked/SKILL.md` |
| Project | `.goose/skills/cooked/SKILL.md` |

### Manual

```bash
mkdir -p ~/.config/goose/skills
cp -R skills/cooked ~/.config/goose/skills/cooked
```

## Invoke

In Goose: `cooked` or describe the post-work audit intent so the skill matches on its description.

## Notes

Standard Agent Skills folder. No Goose-specific adapter.
